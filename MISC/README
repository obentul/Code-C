atexit.c :  Everytime a program run off, before totally end up,it will execute a list of functions(32 registered functions at most),we can register the our self-defined 
            function into the to-be-ran function list with atexit function.

ConverseString.c : 翻转字符串

SnowRobot.c : 雪地机器人问题，通过aswd控制方向，当碰到自己时，报whoops！(双线程，一个线程刷新布局，另一个等待终端输入)
                注：这其中涉及 二维数组，一位数组，自然坐标系的转换(103和104行，为自然坐标系向二维数组的转换，19行为二维数组向一维数组的转换)
