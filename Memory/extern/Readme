使用：extern.c中声明了一个全局变量，只需要使用 gcc x.c extern.c -o x  ， x为相应的数字,比如1.c，2.c等等

1.c   ----   编译不通过，因为在main函数被调用的时候无法找到变量i
2.c   ----   正常输出
3.c   ----   调整extern的位置，放在不同的位置会有不同的情况
4.txt  ----  extern标签的底层实现
5.c  ----  extern标签声明的变量存储位置是否等同于其声明的位置？
