本实验用来验证进程和端口的对应关系，以及子进程是否会占用父进程的端口


使用:gcc Client1.c -o C
     gcc Server1.c -o yks

./yks&

./C 127.0.0.1 6666 1 &


现象：交替输出“father get ...” 和 “child get ...”

结论：子进程会使用父进程的端口，这也说明了端口是"公共资源"，不随进程的改变而改变
